# UniPulse 公网访问修复 - 2026-05-12 19:09

## 问题
用户两次反馈"打不开"。原因：bore 免费隧道不稳定，进程频繁被自动终止。

## 本次操作
1. 检查发现后端 (PID 10152, localhost:8000) 始终正常运行
2. 旧 bore 进程 (48472) 已死亡
3. 多次尝试 bore 启动方案：
   - `bore.bat` + `start /min` → 日志无法写入（工目目录问题）
   - `bore.ps1` → Start-Process -RedirectStandardOutput 编码问题
   - 直接 exec 运行 → 总是被 SIGKILL
4. 最终方案：先用 exec 抓端口号 (20388)，再用 Start-Process -WindowStyle Minimized 启动持久进程 (49736)

## 当前状态
- 后端: PID 10152, localhost:8000 ✅
- Bore: PID 49736, bore.pub:20388 ✅
- 验证全通过: 健康检查/34所高校/前端页面

## 公网地址
http://bore.pub:20388

## 注意事项
- bore 可能随时断开，断开后需重新启动
- 端口每次重连会变