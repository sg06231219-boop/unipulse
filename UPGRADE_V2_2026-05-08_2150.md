# UniPulse 工程化升级 v2.0

**时间**: 2026-05-08 21:30-21:50
**目标**: 将 UniPulse（全球高校风向标）从原始 IIFE + 多 script 标签架构升级为现代工程化架构

## 改造内容

### 1. Vite 构建系统
- 添加 `package.json` (type: module) + `vite.config.js`
- 构建输出：1个 JS bundle (33.46KB gzipped 11.3KB) + 1个 CSS (18.2KB gzipped 4.2KB) + HTML (8KB)
- 10个模块 → 1个 JS 输出（tree-shaking + minify）

### 2. IIFE → ES Modules
- `data.js`: `const DATA = {...}` → `export const DATA = {...}`
- `state.js`: IIFE 闭包 → `export default State`（带事件订阅机制）
- `router.js`: IIFE → ES module，精简为 30 行（委托渲染给 State.setPage）
- `render.js`: `export function renderDiscover/rankings/programs/forum/openDetail/closeModal` + `export default Render`
- `compare.js`: ES module，compute.js 动态 import render.js
- `main.js`: 单入口 `type="module"`，import 所有模块

### 3. 架构简化
- 消除 Router.navigate 和 State.setPage 之间的重复 DOM 操作逻辑
- Router 只负责 hash 解析 + 委托 State.setPage()
- State.setPage 统一管理页面切换（DOM 显隐、nav 状态、渲染）
- 每次导航都触发渲染（不再缓存 _pagesLoaded 跳过），确保状态正确

### 4. PWA 支持
- `public/manifest.json` — PWA 配置（standalone 模式）
- `public/sw.js` — Service Worker（install/activate/fetch 缓存策略）
- 192×192 + 512×512 图标（Pillow 生成）
- `<meta name="theme-color">` + `<link rel="manifest">`
- SW 注册脚本（自动检测支持）

### 5. 全局函数桥接
- `window._showToast` — 替代原全局 showToast
- `window._toggleCompareFromDetail` — 替代原全局 toggleCompareFromDetail
- `window._Compare` — 替代原全局 Compare（供 inline onclick 使用）
- `window.State` — 替代原全局 State（供 inline onclick 使用）

## 验证结果
- ✅ Vite build 成功（10 modules → 1 bundle, 160ms）
- ✅ Dev server 日志正常
- ✅ HTML 包含所有必要元素：module script、manifest、PWA meta、SW registration、page containers
- ✅ dist/ 输出完整：index.html + assets/{js,css} + manifest.json + sw.js + icons

## 文件变更
```
新增: package.json, vite.config.js, public/manifest.json, public/sw.js, public/icon-192.png, public/icon-512.png
重写: js/data.js, js/state.js, js/router.js, js/render.js, js/compare.js, js/main.js
修改: index.html (4处编辑：PWA meta + inline onclick + script tags → module + SW reg)
```

## 启动方式
```bash
cd projects/unipulse
npm run dev     # 开发模式 (http://localhost:3000)
npm run build   # 生产构建 → dist/
npm run preview # 预览生产构建
```