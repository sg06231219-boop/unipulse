# UniPulse Phase1 — 用户认证系统上线

**日期：** 2026-05-13
**公网地址：** http://bore.pub:37611
**登录页面：** http://bore.pub:37611/auth.html

## 502 问题最终结论

排查发现：端口 8000 在本机有异常拦截（返回 502 Content-Length: 0），与 server.py 代码无关。同一份 server.py 在 9999 端口完全正常。

**解决方案：** 改用端口 9999。

## 用户认证系统（新增）

### 后端 API

| 端点 | 方法 | 认证 | 说明 |
|------|------|------|------|
| `/api/auth/register` | POST | 无 | 注册（username+email+password） |
| `/api/auth/login` | POST | 无 | 登录，返回 token |
| `/api/auth/me` | GET | Bearer | 获取当前用户信息 |
| `/api/auth/logout` | POST | Bearer | 退出登录 |

### 数据库变更

- 新增 `users` 表（id, username, email, password_hash, created_at）
- 新增 `sessions` 表（token, user_id, expires_at）
- `forum_posts` 和 `forum_comments` 新增 `user_id` 外键

### 安全特性

- 密码 SHA256 哈希存储
- Session token 32 字节随机
- 过期时间 7 天
- 未认证发帖返回 401
- SQLite WAL + foreign_keys 约束

### 前端登录页

路径：`dist/auth.html`
功能：登录/注册切换表单，登录后显示用户信息，支持跳转回到主站。

## 测试结果（9/9 通过）

- 注册 ✅ | 登录 ✅ | 获取用户 ✅ | 发帖 ✅ | 评论 ✅
- 无认证被拒绝 ✅ | 错误密码拒绝 ✅ | 重复邮箱拒绝 ✅

## 待完成（后续）

- Vue.js 前端集成：现有 dist/ 编译代码需在"发帖/评论"表单中发送 Authorization 头
- 论坛前端改造：用户头像显示、帖子作者链接到用户主页
- AI 选校报告：输入成绩→生成 PDF（免费引流工具）
- 持久化部署脚本：自动启动后端+隧道